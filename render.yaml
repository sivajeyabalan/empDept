services:
  - type: web
    name: emp-dept-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install -g pnpm && pnpm install && pnpm run build
    startCommand: node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
